% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-outlays.R
\name{us_add_outlays}
\alias{us_add_outlays}
\title{Attach annual outlays to a panel}
\usage{
us_add_outlays(panel, funding = NULL, fill_gaps = TRUE, tolerance = 0.25)
}
\arguments{
\item{panel}{A \code{usaspend_panel} built with \code{period = "fiscal"} -- File C
reports federal fiscal periods and has no calendar-year form.}

\item{funding}{Optional prefetched funding table from
\code{\link[=us_fetch_outlays]{us_fetch_outlays()}}; when \code{NULL}, funding is fetched for every award in
the panel (one paged request per award).}

\item{fill_gaps}{Add zero-obligation rows for outlay-years the panel does
not cover.}

\item{tolerance}{Linkage screen: relative gap between File C lifetime
obligations and the award's own allowed before the award is graded
\code{"unlinked"}.}
}
\value{
The panel with \code{outlay_amount} and \code{outlay_coverage} columns, a new
\code{outlays} element (the award x fiscal-year table from
\code{\link[=us_outlays_by_year]{us_outlays_by_year()}}), and \code{meta$outlays} recording the coverage tally.
}
\description{
Adds a cash layer to a fiscal-year panel: \code{outlay_amount} per
org-award-year plus a per-award \code{outlay_coverage} grade, leaving
\code{obligation_net} and \code{net_revenue} untouched -- outlays are a \emph{separate
measure} of the same award, never a substitute (measured on VUMC FY2022+
awards, two-thirds of an award's dollars land in a different year under
outlays than under obligations, and cash lags commitment by about a year).
}
\section{Coverage grades, one per award}{

\describe{
\item{\code{"complete"}}{First panel activity FY2022+ (inside the monthly
reporting mandate), File C obligations reconcile with the award's own
transactions, outlay rows present.}
\item{\code{"truncated_pre_FY2022"}}{Linked and reporting, but the award
predates the FY2022 mandate: pre-mandate outlays were never reported,
so early years are \code{NA} and the lifetime sum is a floor.}
\item{\code{"unlinked"}}{File C obligations differ from the award's
transaction ledger by more than \code{tolerance} -- the outlay values
describe money that cannot be reconciled to this award. Kept, flagged.}
\item{\code{"no_outlay_rows"}}{File C records exist but none carry an outlay
(typical for pre-mandate-only reporters). All \code{outlay_amount} is \code{NA}.}
\item{\code{"no_file_c"}}{The endpoint returned no records at all (VA and DoD
portfolios, mostly). All \code{NA}.}
\item{\code{"fetch_failed"}}{The request errored. All \code{NA} -- a failure is
never read as an empty result.}
}
}

\section{Zero versus NA}{

\code{outlay_amount} is \code{0} only where a zero was plausibly \emph{measured}: years
inside the award's reporting window (FY2022+, or any year with File C
outlay rows) for awards that report outlays. Years before the mandate and
awards with no outlay reporting are \code{NA}, so a missing measurement is
never mistaken for "no cash moved".
}

\section{Trailing cash years}{

Outlays lag obligations, so an award's cash routinely arrives after its
last obligation year -- years the panel has no row for. With \code{fill_gaps = TRUE} (the default, unlike \code{\link[=us_panel]{us_panel()}} where trailing years are the
exception) those years get zero-obligation rows flagged
\code{"outlay_only_year"}, attributed to the award's dominant organization.
With \code{fill_gaps = FALSE} those outlay dollars are dropped and a message
reports how much.
}

\examples{
\dontrun{
p <- us_panel(ex, period = "fiscal")
p <- us_add_outlays(p)
p$panel[outlay_coverage == "complete",
        .(oblig = sum(obligation_net), cash = sum(outlay_amount)), by = year]
}
}
