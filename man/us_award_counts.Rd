% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-search.R
\name{us_award_counts}
\alias{us_award_counts}
\title{Pre-flight award counts for a set of UEIs}
\usage{
us_award_counts(uei, start_date = "2007-10-01", end_date = Sys.Date())
}
\arguments{
\item{uei}{Character vector of UEIs.}

\item{start_date, end_date}{Action-date bounds.}
}
\value{
A \code{data.table}: one row per UEI with \code{contracts}, \code{idvs}, \code{grants},
\code{direct_payments}, \code{loans}, \code{other}, \code{n_awards}, \code{error}, \code{message}.
}
\description{
Cheap reconnaissance before committing to a pull: how many prime awards each
UEI has, by category. Use it to decide between the API and the archive path,
to size download batches, and to distinguish genuine non-recipients from
failed requests.
}
\details{
Failures are recorded, never swallowed. A request that errors comes back with
\code{error = TRUE} and \code{n_awards = NA}, because a failure that reads as zero is
the most damaging silent bug in this pipeline -- it turns a rate-limit blip
into a permanent "this nonprofit gets no federal money".
}
\examples{
\dontrun{
us_award_counts(c("CFFMYPABYAG3", "H7LMD1ANJNN4"))
}
}
