% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-search.R
\name{us_fetch_subawards_batch}
\alias{us_fetch_subawards_batch}
\title{Batched outbound subaward fetch}
\usage{
us_fetch_subawards_batch(awards, batch_size = 400, cap_guard = 9000)
}
\arguments{
\item{awards}{The award spine from \code{\link[=us_normalize_awards]{us_normalize_awards()}}, or any
\code{data.frame} with \code{award_key} and \code{award_id} columns.}

\item{batch_size}{Prime award ids per request (max ~500).}

\item{cap_guard}{Split a batch when its subaward count exceeds this.}
}
\value{
A \code{data.table} matching \code{us_schema("subawards")}, one row per
outbound subaward, with \code{prime_uei} filled from the spine when available
so \code{\link[=us_normalize_subawards]{us_normalize_subawards()}} can classify direction.
}
\description{
The efficient way to get pass-through for a whole extract. Instead of one
request per award, this filters \verb{POST /api/v2/search/spending_by_award/}
with \code{subawards = TRUE} on batches of prime \code{award_ids} -- measured cap:
500 ids per request succeeds, 1,000 returns HTTP 503; the default batch of
400 leaves headroom. On the 50-org pilot this reduced 61,738 per-award
calls to under 200 requests.
}
\details{
Three safeguards, all measured:
\itemize{
\item \strong{Assistance and contract type codes cannot be mixed.} With
\code{subawards = TRUE}, the \emph{result} endpoint returns HTTP 422 when
\code{award_type_codes} spans both families -- while the \emph{count} endpoint
accepts the mix, which makes the failure silent if unhandled. The fetch
therefore runs one pass per family.
\item \strong{The 10k result cap is per filter set.} Each batch is counted
first (\code{spending_by_award_count}, \code{subawards = TRUE}); a batch whose
count nears the cap is split recursively, so nothing silently truncates.
\item \strong{FAINs and PIIDs are ambiguous.} \code{award_ids} matches the bare id,
which different agencies can reuse, so results are filtered back to the
exact \code{prime_award_generated_internal_id} values in \code{awards}. Strays are
dropped, not kept.
}
}
\examples{
\dontrun{
aw <- us_normalize_awards(tx)
out <- us_fetch_subawards_batch(aw)
}
}
