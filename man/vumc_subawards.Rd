% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{vumc_subawards}
\alias{vumc_subawards}
\title{Vanderbilt University Medical Center: subaward rows, both directions}
\format{
A \code{data.table} with 10,394 rows matching \code{us_schema("subawards")}.
Key fields:
\describe{
\item{subaward_key}{Composite dedup key (prime award | subaward number |
action date).}
\item{prime_award_key, prime_award_id}{The prime award the money flows
under.}
\item{prime_uei, prime_name}{The prime awardee (VUMC on outbound rows).}
\item{subawardee_uei, subawardee_name}{Who received the subaward (VUMC on
inbound rows). Often \code{NA} for government subawardees.}
\item{subaward_amount, subaward_action_date, subaward_year,
subaward_fiscal_year}{Committed amount and when it was committed --
the booking basis.}
\item{report_year, report_month, report_last_modified}{FSRS reporting
vintage; lags the action date, sometimes across fiscal years.}
\item{prime_award_amount, prime_awarding_agency_name,
prime_cfda_numbers}{Context carried from the prime award.}
\item{direction}{\code{NA} until \code{\link[=us_normalize_subawards]{us_normalize_subawards()}} assigns it.}
}
}
\usage{
vumc_subawards
}
\description{
All FSRS subaward report lines touching VUMC: 3,212 rows from the
recipient-filtered bulk download (VUMC as \emph{subawardee} -- inbound), plus
7,182 rows fetched by prime award with \code{\link[=us_fetch_subawards_batch]{us_fetch_subawards_batch()}}
(VUMC as \emph{prime} -- outbound pass-through). The two pulls are needed
because a recipient-filtered download never returns outbound rows.
}
\details{
\code{direction} is \code{NA} on purpose: it cannot be inferred from a row alone.
\code{\link[=us_normalize_subawards]{us_normalize_subawards()}} assigns it (\verb{in} / \code{out} / \code{internal} /
\code{unrelated}) given the organization's UEI set, and de-duplicates the FSRS
monthly restatements on \code{subaward_key}.
}
\examples{
sb <- us_normalize_subawards(vumc_subawards,
                             org_uei = unique(vumc_transactions$recipient_uei))
sb[, .(rows = .N, dollars = sum(subaward_amount, na.rm = TRUE)),
   by = direction]
}
\seealso{
\link{vumc_transactions}, \link{vumc_panel}, \code{\link[=us_normalize_subawards]{us_normalize_subawards()}}
}
\keyword{datasets}
