% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subsidiaries.R
\name{us_find_subsidiaries}
\alias{us_find_subsidiaries}
\title{Find subsidiaries surfaced by the parent-UEI match}
\usage{
us_find_subsidiaries(extract)
}
\arguments{
\item{extract}{A \code{usaspend_extract} from \code{\link[=us_extract]{us_extract()}}.}
}
\value{
A \code{data.table}, one row per subsidiary UEI: \code{uei}, \code{org_id} (the
requested UEI it rolls up to, until \code{\link[=us_panel]{us_panel()}} applies an \code{org_map}),
\code{relationship}, \code{parent_uei} (the recorded parent), \code{root_uei} (the
requested UEI at the top of the chain), \code{recipient_name}, \code{extracted}
(whether its own full history is in the extract), and
\code{n_parent_matched} (transactions that arrived through the parent match).
}
\description{
The API recipient filter also matches a transaction's \emph{parent} UEI (see
\code{\link[=us_download_submit]{us_download_submit()}}), so an extract holds some transactions of the
requested organizations' subsidiaries -- only those filed while a requested
UEI was recorded as their parent. This lists them: every unrequested
recipient whose \code{recipient_parent_uei} is a requested UEI (or, recursively,
an already-found subsidiary).
}
\details{
\code{\link[=us_extract]{us_extract()}} records the result in \code{extract$org_map}; this function
rebuilds it for any extract, including ones made before the crosswalk
existed. See \code{vignette("org-map")}.
}
\examples{
us_find_subsidiaries(us_sample_extract())   # none in the sample
}
