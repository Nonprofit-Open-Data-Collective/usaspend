% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disruption.R
\name{us_disruption_flags}
\alias{us_disruption_flags}
\title{Flag disruption signals on a transaction ledger}
\usage{
us_disruption_flags(transactions, early_days = 60L, cut_days = 30L)
}
\arguments{
\item{transactions}{A \code{data.table} matching \code{us_schema("transactions")},
ideally from \code{\link[=us_normalize_transactions]{us_normalize_transactions()}}. The description and ceiling
fields must be present for the text and ceiling flags; extracts
harmonized before those fields existed get \code{FALSE} there.}

\item{early_days}{Days before the scheduled end within which a
de-obligation still counts as routine.}

\item{cut_days}{Minimum end-date move, in days, for a cut or extension.}
}
\value{
The input, ordered by award and action date, with the \verb{dsr_*}
logical columns and the helper columns added.
}
\description{
Adds one logical column per disruption signal to a canonical transaction
table, organized by the four-part taxonomy of \code{vignette("disruption")}.
Flags are per \emph{action}; aggregate them by award, agency, or period to
measure disruption. Nothing is dropped or netted.
}
\details{
The flags are deliberately literal -- each is one observable property of a
single action -- so they can be combined, audited against the description
text, and compared against a pre-period baseline. None of them is proof of
a policy decision on its own: an end-date cut is routine on a supply order,
and closeouts happen every year. Disruption is a \emph{change in the rate} of
these actions, which is how the vignette uses them.
}
\section{Termination signals}{

\describe{
\item{\code{dsr_term_code}}{Contract action type \code{E} (terminate for default),
\code{F} (terminate for convenience) or \code{N} (legal contract cancellation).
Assistance has no termination code.}
\item{\code{dsr_term_text}}{Termination or cancellation language in
\code{transaction_description} (excluding "determination" and clause
citations). The only formal termination signal for grants.}
\item{\code{dsr_rescinded}}{Language rescinding or reinstating a termination.}
\item{\code{dsr_closeout}}{Contract action type \code{K} (close out).}
}
}

\section{Reduction signals}{

\describe{
\item{\code{dsr_early_deob}}{Negative obligation dated more than
\code{early_days} before the scheduled end the action interrupts -- money
withdrawn from a live award, not a post-performance closeout.}
\item{\code{dsr_end_cut}}{The period-of-performance end moved \emph{earlier} by more
than \code{cut_days} and by at least a quarter of the remaining schedule.}
\item{\code{dsr_ceiling_cut}}{Negative \code{base_and_all_options_value}: the
contract's potential value reduced (contracts only).}
}
}

\section{Delay signals}{

\describe{
\item{\code{dsr_extension}}{End date moved \emph{later} by more than \code{cut_days}.}
\item{\code{dsr_stop_work}}{Stop-work or suspension language in the description.}
\item{\code{dsr_admin}}{A zero-dollar action classed administrative -- the
category stop-work orders and notices are usually filed under.}
}
Missed or late payments are properties of the \emph{gaps between} actions and of
File C outlays, not of any single row; the vignette measures them at the
award level.
}

\section{Transfer signals}{

\describe{
\item{\code{dsr_novation}}{Contract action type \code{J}: same contract, new vendor.}
\item{\code{dsr_transfer}}{Contract action type \code{T} (transfer action), used to
move an award's funds between agencies.}
\item{\code{dsr_agency_change}}{Awarding agency differs from the award's
previous action -- how an agency closure appears (USAID -> State).}
\item{\code{dsr_recipient_change}}{Recipient UEI differs from the award's
previous action.}
}
}

\section{Helper columns}{

\code{sched_end} (the end date in force before this action), \code{end_shift_days}
(this action's end date minus that), and \code{ceiling_change} (the numeric
ceiling delta, \code{NA} for assistance).
}

\examples{
tx <- us_normalize_transactions(us_sample_extract()$transactions)
f <- us_disruption_flags(tx)
f[, lapply(.SD, sum), .SDcols = patterns("^dsr_")]
}
