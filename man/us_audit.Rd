% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile.R
\name{us_audit}
\alias{us_audit}
\title{Audit a finished panel}
\usage{
us_audit(panel)
}
\arguments{
\item{panel}{A \code{usaspend_panel} from \code{\link[=us_panel]{us_panel()}}.}
}
\value{
A \code{data.table} of \code{check}, \code{value}, \code{status} (\code{"ok"} / \code{"info"} /
\code{"warn"}).
}
\description{
Structural checks needing no external reference. Run on every rebuild and
diff against the previous run, so a change in the data or a rule shows up as
a change in the audit rather than as a quietly different number.
}
\examples{
p <- us_panel(us_sample_extract())
us_audit(p)
}
