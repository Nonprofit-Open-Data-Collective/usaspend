% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-outlays.R
\name{us_fetch_outlays}
\alias{us_fetch_outlays}
\title{Fetch account-level (File C) funding history per award}
\usage{
us_fetch_outlays(award_key, page_limit = 100)
}
\arguments{
\item{award_key}{Character vector of generated unique award ids.}

\item{page_limit}{Records per page (API maximum 100).}
}
\value{
A \code{data.table} matching \code{us_schema("funding")}, with a
\code{usaspend_failed} attribute naming awards whose fetch errored.
}
\description{
Walks \verb{POST /api/v2/awards/funding/} for each award: one row per federal
account x object class x program activity x reporting period, carrying the
two account-level money fields. This is the \strong{only} source of annual
outlays -- the award data itself is lifetime-only (see \code{\link[=us_money_column]{us_money_column()}}).
}
\section{The two money columns are on different bases}{

\code{transaction_obligated_amount} is \strong{incremental} per period.
\code{gross_outlay_amount} is \strong{cumulative within each fiscal year} per account
cell (File C reports it as of period end, resetting at the fiscal year
boundary). Summing it across periods double-counts; \code{\link[=us_outlays_by_year]{us_outlays_by_year()}}
applies the correct last-period-per-year rule.
}

\section{Failures are not empty results}{

An award whose request errors is recorded in the \code{usaspend_failed}
attribute and warned about -- it must not be read as "this award has no
File C data". Awards that genuinely return zero records are simply absent
from the result.
}

\examples{
\dontrun{
fc <- us_fetch_outlays("ASST_NON_NU2GGH002367_075")
us_outlays_by_year(fc)
}
}
