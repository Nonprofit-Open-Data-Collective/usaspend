% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rollup.R
\name{us_rollup}
\alias{us_rollup}
\title{Roll the panel up across grouping dimensions}
\usage{
us_rollup(panel, org_id = TRUE, year = NULL, state = NULL)
}
\arguments{
\item{panel}{A \code{usaspend_panel} from \code{\link[=us_panel]{us_panel()}}.}

\item{org_id}{Group by organization. Default \code{TRUE}.}

\item{year}{Group by year. Default \code{NULL} (aggregate across years);
\code{TRUE} produces a trend tabulation.}

\item{state}{Group by recipient state. Default \code{NULL}.}
}
\value{
A \code{data.table} at the requested grain, sorted by the grouping keys.
}
\description{
Aggregates every award in the panel to the chosen grain, carrying both
directions of money: prime obligations, outflows (pass-through subawards
paid), and inflows (subawards received, which never appear in prime data).
}
\details{
Each dimension is a toggle: \code{TRUE} groups by it, \code{NULL} (or \code{FALSE})
aggregates over it.
\describe{
\item{\verb{org_id = TRUE, year = NULL}}{(default) one row per organization,
totalled across all years.}
\item{\code{year = TRUE}}{a trend tabulation -- org x year, or year alone if
\code{org_id = FALSE}.}
\item{\code{state = TRUE}}{groups prime flows by the award's registered
recipient state. Inbound subawards are organization-level, so they are
assigned to the organization's dominant state (largest share of absolute
net obligations).}
}
All three \code{FALSE}/\code{NULL} returns a single grand-total row.
}
\section{Columns}{

Counts (\code{n_awards}, \code{n_transactions}, \code{n_subawards_out}, \code{n_subawards_in})
and dollars: gross positive / negative / net obligations, loan face value,
\code{subaward_out_amount}, \code{subaward_in_amount}, \code{net_revenue}
(\code{obligation_net - subaward_out_amount}) and \code{total_net}
(\code{obligation_net + subaward_in_amount - subaward_out_amount}) -- the fullest
single measure of net federal dollars flowing to the group.

Inflows come from the panel's organization-level \code{subawards_in} table, not
from the panel rows (panel rows carry inbound amounts only in the rare case
where an organization is prime and subawardee on the same award).
}

\examples{
p <- us_panel(us_sample_extract())
us_rollup(p)                             # one row per org, all years
us_rollup(p, year = TRUE)                # org x year trend
us_rollup(p, org_id = FALSE, year = TRUE)  # sector-wide trend
us_rollup(p, state = TRUE, org_id = FALSE) # state totals
}
