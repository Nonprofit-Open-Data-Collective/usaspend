% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codes.R
\name{us_classify_award_type}
\alias{us_classify_award_type}
\title{Classify award type codes}
\usage{
us_classify_award_type(award_type_code, idv_type_code = NULL)
}
\arguments{
\item{award_type_code}{Character vector of award type codes.}

\item{idv_type_code}{Optional character vector of IDV type codes, used where
\code{award_type_code} is missing. Accepted bare (\code{"B"}) or prefixed
(\code{"IDV_B"}).}
}
\value{
A \code{data.table} with one row per input and columns \code{award_type_code},
\code{award_type_label}, \code{award_family}, \code{award_group}.
}
\description{
Maps a USAspending award type code to a stable label, an award family
(contract / idv / grant / direct_payment / loan / other) and the top-level
group (contract vs assistance).
}
\details{
Contract IDV rows carry a blank award type code -- their type lives in
\code{idv_type_code} instead. Pass \code{idv_type_code} and it is used as a fallback.
Note that FPDS writes the IDV type as a bare letter (\code{"B"}, \code{"E"}), which
collides with the contract codes for Purchase Order and Definitive Contract,
so the fallback prefixes \code{IDV_} before looking the code up.
}
\examples{
us_classify_award_type(c("04", "D", "IDV_B", NA))
}
