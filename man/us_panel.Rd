% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel.R
\name{us_panel}
\alias{us_panel}
\title{Build the organization x award x year panel}
\usage{
us_panel(
  extract,
  org_map = NULL,
  period = c("calendar", "fiscal"),
  measure = c("obligation", "pragmatic"),
  deobligation_policy = c("as_posted", "restate", "drop"),
  fill_gaps = FALSE
)
}
\arguments{
\item{extract}{A \code{usaspend_extract} from \code{\link[=us_extract]{us_extract()}}, or a list with
\code{transactions} and \code{subawards}.}

\item{org_map}{Optional \code{uei} to \code{org_id} crosswalk, see \code{\link[=us_org_map]{us_org_map()}}.}

\item{period}{\code{"calendar"} (default) or \code{"fiscal"}.}

\item{measure}{Money measure, see \code{\link[=us_money_column]{us_money_column()}}.}

\item{deobligation_policy}{See \code{\link[=us_net_by_year]{us_net_by_year()}}.}

\item{fill_gaps}{Emit zero-obligation rows so every dollar of pass-through
has a row to land on. This does two things: interior years of an award's
life with no actions get zero rows (via \code{\link[=us_net_by_year]{us_net_by_year()}}), and outbound
subawards booked to an org-award-year with no prime activity -- common,
because FSRS reports trail the obligations they draw on -- get their own
zero-obligation rows instead of being silently dropped. In the pilot, 13\%
of outbound dollars fell in such years. With \code{fill_gaps = FALSE} the
panel's \code{subaward_out_amount} total is a floor, not the total fetched.}
}
\value{
A list of class \code{usaspend_panel}: \code{panel} (org x award x year),
\code{awards} (the spine), \code{transactions} (the normalized ledger),
\code{subawards} (normalized, with direction), \code{subawards_in} (org-year
inbound revenue), and \code{meta}.
}
\description{
The end-to-end pipeline: normalize, ledger, net, join subaward flows, attach
award attributes.
}
\section{What the panel measures}{

\code{obligation_net} is the net federal obligation booked to the
organization-award-year. It is a \strong{commitment}, not a disbursement --
federal award data does not carry annual cash. \code{net_revenue} is
\code{obligation_net - subaward_out_amount}: what the organization commits to
keeping after money it is obliged to pass through. Outbound subawards are
present only if the extract fetched them (\code{subawards = "out"} or \code{"both"} in
\code{\link[=us_extract]{us_extract()}}); otherwise the column is zero and \code{subaward_coverage} says
\code{"not_fetched"}, so a zero is never mistaken for a measured zero.

Inbound subawards -- money received as a subrecipient, invisible in prime
data -- are aggregated per organization-year in the separate
\code{subawards_in} element, keyed by subawardee UEI, and also joined onto
matching panel rows as \code{subaward_in_amount} when the prime award key appears
in the panel (rare: it requires the org to be both prime and subawardee).
}

\examples{
p <- us_panel(us_sample_extract())
p$panel[, .(rows = .N, net = sum(obligation_net)), by = year][order(year)]
}
