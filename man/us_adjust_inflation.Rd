% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rollup.R
\name{us_adjust_inflation}
\alias{us_adjust_inflation}
\title{Adjust panel dollars for inflation}
\usage{
us_adjust_inflation(
  x,
  target_year = NULL,
  index = NULL,
  cols = NULL,
  strict = TRUE
)
}
\arguments{
\item{x}{A \code{usaspend_panel} (its \code{panel} and \code{subawards_in} tables are both
adjusted), or any \code{data.frame} with a \code{year} column -- including
\code{\link[=us_rollup]{us_rollup()}} output, provided it was built with \code{year = TRUE} (a rollup
collapsed across years mixes vintages and cannot be deflated; adjust
first, then roll up).}

\item{target_year}{Year whose dollars to state everything in. Default: the
latest year in the data that the index covers (a message says which).}

\item{index}{\code{NULL} for the bundled CPI-U series (\code{\link[=us_price_index]{us_price_index()}}), or a
\code{data.frame} of \code{year} and \code{index}.}

\item{cols}{Columns to adjust. Default: the package's known dollar columns
present in the data.}

\item{strict}{If \code{TRUE} (default), a data year missing from the index is an
error. If \code{FALSE}, those rows' dollar columns become \code{NA} with a warning --
visible, never silently unadjusted.}
}
\value{
The same structure with dollar columns restated, and an
\code{usaspend_inflation} attribute (on the data.frame, or in \code{meta} for a
panel) recording the target year and index range used.
}
\description{
Restates every dollar column into constant dollars of a target year using an
annual price index: \code{amount * index[target] / index[year]}.
}
\examples{
p <- us_panel(us_sample_extract())
oy <- us_rollup(p, year = TRUE)
oy25 <- us_adjust_inflation(oy, target_year = 2025)
attr(oy25, "usaspend_inflation")
}
