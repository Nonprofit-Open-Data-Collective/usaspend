% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{us_harmonize_transactions}
\alias{us_harmonize_transactions}
\alias{us_harmonize_subawards}
\title{Harmonize raw USAspending files to the canonical schema}
\usage{
us_harmonize_transactions(
  raw,
  group = c("assistance", "contract"),
  source_file = NA_character_
)

us_harmonize_subawards(
  raw,
  group = c("assistance", "contract"),
  source_file = NA_character_
)
}
\arguments{
\item{raw}{A \code{data.frame}/\code{data.table} of raw USAspending columns, read as
character.}

\item{group}{\code{"assistance"} or \code{"contract"}.}

\item{source_file}{Optional provenance label carried onto every row.}
}
\value{
A \code{data.table} matching \code{us_schema("transactions")} or
\code{us_schema("subawards")}.
}
\description{
Both acquisition paths -- the REST download endpoint and the annual Award
Data Archive -- emit the same underlying CSV layouts, so one harmonizer
serves both. Raw columns are read as character and cast explicitly here;
nothing is guessed by a CSV reader.
}
\details{
One measured divergence between the paths is repaired here: archive
\emph{contract} files transpose the \code{action_type_code}/\code{action_type} and
\code{idv_type_code}/\code{idv_type} column pairs (codes and descriptions swap
places). The swap is detected from the values themselves and undone before
mapping, so both sources classify identically.

Derived on the way through:
\itemize{
\item \code{award_group} from the file family (contract vs assistance).
\item \code{award_type_label} / \code{award_family} from the code, with \code{idv_type_code}
as a fallback because contract IDV rows carry a blank award type code.
\item \code{action_type_label} / \code{action_class} from the code \emph{and} the family,
because the codes collide across families.
\item \code{action_year} (calendar) and \code{action_fiscal_year} from \code{action_date}.
The fiscal year is recomputed rather than trusting the source column.
\item \code{pragmatic_obligation}, which USAspending supplies for assistance
(\code{generated_pragmatic_obligations}, equal to the loan subsidy cost for
loans and to the obligation otherwise) but not for contracts, where the
obligation is copied in.
}

This is a mechanical reshape. It applies no accounting rules -- no
de-duplication, no correction or delete handling, no netting. Those live in
\code{\link[=us_normalize_transactions]{us_normalize_transactions()}} and are specified in \code{ACCOUNTING.md}.
}
