% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{us_normalize_transactions}
\alias{us_normalize_transactions}
\title{Normalize a raw transaction ledger}
\usage{
us_normalize_transactions(
  transactions,
  requested_uei = NULL,
  drop_aggregates = TRUE
)
}
\arguments{
\item{transactions}{A \code{data.table} matching \code{us_schema("transactions")}.}

\item{requested_uei}{Optional character vector of the UEIs actually asked
for, used to flag strays returned by the API's text matching.}

\item{drop_aggregates}{Drop assistance \code{record_type_code} 1.}
}
\value{
A \code{data.table} matching \code{us_schema("transactions")} plus
\code{is_zero_dollar}, \code{is_deobligation}, \code{is_stray_uei}, \code{is_legacy_cdi} and a
character \code{flags} column. Carries a \code{usaspend_dropped} attribute recording
what was removed and why.
}
\description{
Turns harmonized-but-raw transactions into a clean ledger, one row per real
award action, ready for aggregation.
}
\section{What it does}{

\enumerate{
\item \strong{De-duplicates on \code{transaction_key}.} Overlapping download batches
produce exact duplicates. In the pilot, 19,445 of 241,118 assistance rows
(8.1\%) and 1,383 of 60,907 contract rows (2.3\%) were duplicates, and
every single one was a byte-identical copy -- same amount, same
\code{last_modified_date}. De-duplicating recovered 5,754 awards that had
previously failed the lifetime reconciliation check.
\item \strong{Drops deleted records.} \code{correction_delete_code == "D"} means the
record was withdrawn. Note that \code{"C"} does \emph{not} mean "superseded by
something else" -- it means this row is itself the correction, and it is
the current version, so it is kept. 37.6\% of pilot assistance rows carry
\code{"C"}. A pipeline that drops them would discard a third of the data.
\code{"L"} is an undocumented legacy value; in the pilot all 1,719 instances
are Department of Energy records dated 2008-2017. They are kept and
flagged.
\item \strong{Drops aggregate records} (\code{record_type_code == 1}), which are
county-level rollups with no identified recipient and would double-count.
\item \strong{Keeps zero-dollar actions, flagged.} They are 20\% of pilot
assistance transactions and 30\% of contract transactions. They carry
period-of-performance changes and are how activity is counted.
\item \strong{Flags anomalies} rather than silently repairing them.
}
}

\examples{
tx <- us_normalize_transactions(us_sample_extract()$transactions)
attr(tx, "usaspend_dropped")
}
