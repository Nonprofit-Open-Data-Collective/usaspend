% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{us_impute_outlays}
\alias{us_impute_outlays}
\title{Impute annual outlays from obligations data}
\usage{
us_impute_outlays(transactions, model = NULL)
}
\arguments{
\item{transactions}{A \code{data.table} matching \code{us_schema("transactions")},
or a precomputed feature table from \code{\link[=us_outlay_features]{us_outlay_features()}}.}

\item{model}{A \code{usaspend_outlay_model}; \code{NULL} uses the bundled
\link{outlay_model}, fitted on the packaged experiment's 1,189 ground-truth
awards.}
}
\value{
A \code{data.table}, one row per award x fiscal year: \code{outlay_imputed}
(dollars), event time \code{t}, \code{imputation_method}
(\code{"liquidation_curve"} / \code{"even_spread"} / \code{"none"}) and
\code{imputation_flags}.
}
\description{
Allocates each award's net obligations across fiscal years as imputed
cash, using a fitted liquidation-curve model where the award's data
supports it and an explicit fallback where it does not. Dollars are never
\code{NA}: awards outside the model's support envelope get the basic rule
\emph{global outlay/obligation ratio x (net obligations / performance
periods)}, and \code{imputation_method} says which rule produced each row.
}
\section{When the model steps aside}{

The model path requires a first-obligation year, positive net
obligations, and a duration the training data supports (at least
\code{min_cell} ground-truth awards of that duration -- the support envelope).
Everything else -- missing period-of-performance dates, a missing start
month, durations beyond the envelope -- falls back to even spread, with
the reason in \code{imputation_flags}. An award still in progress is \emph{not} a
fallback case: the curve projects its remaining cash, including fiscal
years after the data pull; those rows simply carry future \code{fy} values.
}

\examples{
tx <- us_normalize_transactions(us_sample_extract()$transactions)
imp <- us_impute_outlays(tx)
imp[, .(dollars = sum(outlay_imputed)), by = imputation_method]
}
