% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subsidiaries.R
\name{us_add_subsidiaries}
\alias{us_add_subsidiaries}
\title{Extract the full histories of an extract's subsidiaries}
\usage{
us_add_subsidiaries(extract, max_rounds = 3L, dest = us_cache_dir("raw"))
}
\arguments{
\item{extract}{A \code{usaspend_extract} from \code{\link[=us_extract]{us_extract()}}.}

\item{max_rounds}{A subsidiary's own pull can surface subsidiaries of its
own; they are pulled in further rounds, up to this many in total.}

\item{dest}{Directory for intermediate files. Defaults to the package cache.}
}
\value{
The extract with transactions (and, per \code{meta$subawards},
subawards) added, \code{org_map} updated, and \code{meta$uei} extended;
\code{meta$uei_requested} keeps the original request.
}
\description{
Pulls, through the API, every subsidiary UEI in the extract's crosswalk
whose own history is not yet in it, and adds them to the requested set so
\code{\link[=us_panel]{us_panel()}} counts them as part of their parent organization. Equivalent
to having run \code{\link[=us_extract]{us_extract()}} with \code{subsidiaries = TRUE}, without repeating
the main pull.
}
\details{
A query on the subsidiary's own UEI returns its whole history, including
years before the parent relationship existed (for an acquired firm, the
years before the acquisition). Those years are counted to the parent
organization; see \code{vignette("org-map")} for how to drop them.
}
\examples{
\dontrun{
ex <- us_extract("JJHCMK4NT5N3", years = 2008:2025)   # RTI
us_find_subsidiaries(ex)                               # IRG, among others
ex <- us_add_subsidiaries(ex)
}
}
