% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{us_org_map}
\alias{us_org_map}
\title{Map UEIs to organizations}
\usage{
us_org_map(uei, org_map = NULL)
}
\arguments{
\item{uei}{Character vector of UEIs.}

\item{org_map}{Optional two-column \code{data.frame} of \code{uei} and \code{org_id}
(an EIN, for instance).}
}
\value{
A \code{data.table} of \code{uei}, \code{org_id}.
}
\description{
Large nonprofits hold several SAM registrations and USAspending splits their
awards across them, so the organization -- not the UEI -- is the unit of
analysis. Supply a crosswalk and every downstream table is keyed on
\code{org_id}. Without one, each UEI is its own organization.
}
\examples{
us_org_map(c("CFFMYPABYAG3", "H7LMD1ANJNN4"))
}
