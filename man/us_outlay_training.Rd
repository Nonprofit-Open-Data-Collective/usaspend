% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{us_outlay_training}
\alias{us_outlay_training}
\title{Build an outlay-imputation training set}
\usage{
us_outlay_training(
  transactions,
  funding = NULL,
  min_first_fy = 2020L,
  min_oblig = 50000,
  as_of = NULL
)
}
\arguments{
\item{transactions}{A \code{data.table} matching \code{us_schema("transactions")}.}

\item{funding}{Optional prefetched File C table from \code{\link[=us_fetch_outlays]{us_fetch_outlays()}};
fetched (one paged request per candidate award) when \code{NULL}.}

\item{min_first_fy}{Earliest first-obligation fiscal year to consider.
File C coverage begins in earnest FY2020; earlier awards cannot be truth.}

\item{min_oblig}{Minimum absolute net obligation.}

\item{as_of}{The current (incomplete) federal fiscal year; defaults to
today's. Used by the censoring screens.}
}
\value{
A list of class \code{usaspend_outlay_training}: \code{awards} (features +
File C lifetime figures + \code{tier}), \code{grid} (award x fiscal year rows for
truth awards: \code{oblig_fy}, \code{actual}, event time \code{t}), and \code{meta}.
}
\description{
Assembles the ground truth the imputation model is fitted on: award
features from the transaction ledger, annual File C outlays per award,
and the two-tier truth screen from the imputation experiment
(\code{IMPUTATION.md} 1). Awards that fail the screen are kept in \code{awards}
with \code{tier = NA} but excluded from \code{grid}.
}
\section{The two tiers}{

Both require \strong{linkage} -- File C lifetime obligations within 10\% of the
award's own ledger. \code{"reconciled"} additionally requires lifetime outlays
within 10\% of lifetime obligations with cash no longer flowing.
\code{"shape_complete"} requires a first obligation FY2022+ (inside the monthly
reporting mandate), performance ended before the current fiscal year, and
a plateaued outlay series -- the timing profile is fully observed even
where the level fell short.
}

\examples{
\dontrun{
tr <- us_outlay_training(us_normalize_transactions(vumc_transactions))
}
}
