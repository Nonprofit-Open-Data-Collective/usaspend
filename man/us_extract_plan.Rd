% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{us_extract_plan}
\alias{us_extract_plan}
\title{Plan an extraction before running it}
\usage{
us_extract_plan(
  uei,
  years = 2008:2025,
  batch_size = us_opt("batch_size"),
  concurrent = us_opt("concurrent"),
  mbps = 25
)
}
\arguments{
\item{uei}{Character vector of UEIs.}

\item{years}{Integer vector of fiscal years to cover.}

\item{batch_size, concurrent}{Overrides for the API-path cost model.}

\item{mbps}{Assumed sustained download throughput, for the archive path.}
}
\value{
A \code{data.table} comparing the paths, invisibly; the recommendation is
printed and returned in the \code{recommended} attribute.
}
\description{
Compares the two acquisition paths for a given input and recommends one. The
API path costs time proportional to the number of UEIs; the archive path
costs a fixed volume proportional to the number of fiscal years. They cross
over somewhere in the low thousands of UEIs.
}
\details{
The estimates are order-of-magnitude, built from measured constants:
a bulk-download job took 9-85 seconds server-side, \code{recipient_search_text}
caps near 20 values (and large recipients fail well below that, hence a
default batch of 5), and \code{FY2024_All_Assistance_Full} is 1.37 GB compressed.
}
\examples{
us_extract_plan(rep("CFFMYPABYAG3", 40), years = 2015:2025)
}
