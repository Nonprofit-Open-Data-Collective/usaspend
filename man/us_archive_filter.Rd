% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive.R
\name{us_archive_filter}
\alias{us_archive_filter}
\title{Filter unpacked archives down to a set of UEIs}
\usage{
us_archive_filter(
  uei,
  csv_dir,
  group = c("assistance", "contract"),
  memory_limit = "8GB",
  parquet_out = NULL
)
}
\arguments{
\item{uei}{Character vector of UEIs to keep.}

\item{csv_dir}{One or more directories of unpacked archive CSVs.}

\item{group}{\code{"assistance"} or \code{"contract"} -- which family these files hold.}

\item{memory_limit}{duckdb memory ceiling.}

\item{parquet_out}{Optional path; if given, the filtered rows are also
written to parquet so later runs skip the CSV scan entirely.}
}
\value{
A \code{data.table} matching \code{us_schema("transactions")}.
}
\description{
Streams the archive CSVs through duckdb, keeps only rows whose
\code{recipient_uei} is in \code{uei}, and harmonizes the result to
\code{us_schema("transactions")}. Memory stays bounded because duckdb reads the
CSVs lazily and the UEI list is joined as a table rather than pasted into a
giant \code{IN} clause.
}
