% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{us_impute_eval}
\alias{us_impute_eval}
\title{Cross-validated evaluation of an outlay-imputation configuration}
\usage{
us_impute_eval(
  training,
  cells = c("dur_bin", "late_start"),
  min_cell = 8L,
  folds = 5L,
  seed = 1L
)
}
\arguments{
\item{training}{A \code{usaspend_outlay_training}.}

\item{cells, min_cell}{Passed to \code{\link[=us_impute_fit]{us_impute_fit()}}.}

\item{folds}{Number of CV folds.}

\item{seed}{RNG seed for fold assignment.}
}
\value{
A list: \code{summary} (mean/median/dollar-weighted by method and
metric), \code{by_class} (mean timing misallocation by \code{mod_class}), and
\code{scores} (per award).
}
\description{
K-fold (by award) evaluation of the liquidation-curve model against the
two reference rules: \code{as_obligated} (cash booked in the commitment year)
and \code{even_spread} (the fitted global ratio spread evenly over the
performance window). Reports both metrics from the experiment: \code{timing}
(misallocation share, imputed series rescaled to the actual total) and
\code{level_timing} (no rescaling -- the method is charged for the level too).
}
\examples{
ev <- us_impute_eval(outlay_training)
ev$summary
}
