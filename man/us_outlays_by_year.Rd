% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-outlays.R
\name{us_outlays_by_year}
\alias{us_outlays_by_year}
\title{Collapse File C funding records to annual outlays per award}
\usage{
us_outlays_by_year(funding)
}
\arguments{
\item{funding}{A \code{data.table} matching \code{us_schema("funding")}, from
\code{\link[=us_fetch_outlays]{us_fetch_outlays()}}.}
}
\value{
A \code{data.table} of \code{award_key}, \code{fiscal_year}, \code{outlay}
(NA when the award-year reported no outlay rows, never a fake zero),
\code{filec_obligation}, and \code{n_periods}.
}
\description{
Turns the period-level funding table into one row per award x federal
fiscal year, applying the cumulative-within-year rule that
\code{gross_outlay_amount} requires: within each account cell (federal account x
DEFC x object class x program activity x funding agency) the annual outlay
is the \strong{last reported value} of the fiscal year, and the award-year
outlay is the sum over cells. \code{transaction_obligated_amount} is
incremental, so it sums directly; it is returned as \code{filec_obligation} so
the caller can check File C against the award's own transaction ledger --
the linkage screen \code{\link[=us_add_outlays]{us_add_outlays()}} runs per award.
}
\details{
Years are \strong{federal fiscal years}; File C has no calendar-year form.
}
