% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{us_add_imputed_outlays}
\alias{us_add_imputed_outlays}
\title{Attach imputed outlays to a panel}
\usage{
us_add_imputed_outlays(
  panel,
  model = NULL,
  fill_gaps = TRUE,
  reconcile = FALSE
)
}
\arguments{
\item{panel}{A \code{usaspend_panel} built with \code{period = "fiscal"} (curves
are fitted in fiscal event time).}

\item{model}{A \code{usaspend_outlay_model}; \code{NULL} uses the bundled
\link{outlay_model}.}

\item{fill_gaps}{Add rows for imputed-cash years the panel lacks.}

\item{reconcile}{Rescale each award's imputed series to sum exactly to
its net obligations -- see \code{\link[=us_impute_outlays]{us_impute_outlays()}}.}
}
\value{
The panel with \code{outlay_imputed} and \code{imputation_method} columns
and \code{meta$imputation} recording the model and method tally.
}
\description{
Runs \code{\link[=us_impute_outlays]{us_impute_outlays()}} on the panel's own transaction ledger and
joins the result onto the org x award x year table as \code{outlay_imputed},
with \code{imputation_method} per award. Money measures already in the panel
are never touched. Rows are added (flagged \code{imputed_outlay_only_year})
for fiscal years the curve allocates cash to but the panel has no row
for -- including \emph{future} years of awards still in progress, which is
the point of imputation.
}
\examples{
p <- us_panel(us_sample_extract(), period = "fiscal")
p <- us_add_imputed_outlays(p)
p$panel[, .(oblig = sum(obligation_net), imputed = sum(outlay_imputed))]
}
