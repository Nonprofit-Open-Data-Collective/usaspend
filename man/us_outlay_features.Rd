% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{us_outlay_features}
\alias{us_outlay_features}
\title{Award-level features for outlay imputation}
\usage{
us_outlay_features(transactions)
}
\arguments{
\item{transactions}{A \code{data.table} matching \code{us_schema("transactions")}
(normalized or raw canonical).}
}
\value{
One row per award: identifiers, \code{first_fy}, \code{last_oblig_fy},
\code{first_month} (fiscal month of the first action, Oct = 1),
\code{late_start} (first obligated Apr-Sep), obligation totals,
period-of-performance fields, \code{duration}, \code{dur_bin} (capped at 6),
extension/reduction booleans and \code{mod_class}.
}
\description{
Collapses a transaction ledger to one row per award carrying the traits
the imputation model uses (duration, start timing) and the modification
taxonomy defined by the imputation experiment (see \code{IMPUTATION.md} 6.4).
}
\section{The modification taxonomy}{

\code{mod_class} is one label per award, first match wins:
\describe{
\item{\code{reduced}}{De-obligations exceed 5\% of gross positive.}
\item{\code{extension_funded}}{Final \code{pop_end_date} pushed more than 90 days
past the first reported one, with material new money (> 5\% of prior
positives) at or after the extension event.}
\item{\code{extension_timeline}}{Extended as above without material new
money -- a no-cost extension.}
\item{\code{multi_year_incremental}}{Obligations in more than one fiscal
year, no material extension or reduction.}
\item{\code{single_year}}{All obligations in one fiscal year.}
}
}

\examples{
f <- us_outlay_features(us_sample_extract()$transactions)
f[, .N, by = mod_class]
}
