% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{us_fetch_subawards_in}
\alias{us_fetch_subawards_in}
\title{Append inbound subawards to an archive extract}
\usage{
us_fetch_subawards_in(uei, years, dest = us_cache_dir("raw"))
}
\arguments{
\item{uei}{Character vector of UEIs.}

\item{years}{Integer vector of fiscal years.}

\item{dest}{Directory for intermediate files; subaward CSVs land in a
\verb{subawards_in/} subdirectory so they never mingle with other downloads.}
}
\value{
A \code{data.table} matching \code{us_schema("subawards")}. Direction is left
unset, as in any bulk pull -- \code{\link[=us_normalize_subawards]{us_normalize_subawards()}} assigns it from
the organization's UEI set.
}
\description{
The annual Award Data Archive carries no subawards in either direction, so
an archive-path extract has no inbound rows -- the revenue an organization
receives as a \emph{subawardee}, which appears nowhere in prime data. This
fetches them through the API.
}
\details{
There is no cheaper route: a \code{sub_award_types}-only custom download job was
probed live and accepted, but builds far slower server-side than the
standard transactions job whose by-product already includes the same rows.
So this simply runs the standard bulk-download jobs and harvests only the
subaward files -- \strong{the cost is the API path's cost}, roughly one job per
\code{batch_size} UEIs. For a handful of organizations that is minutes; for the
full-crosswalk scale that motivated the archive path in the first place it
is not, which is why \code{\link[=us_extract]{us_extract()}} only calls this when \code{subawards} is
explicitly \code{"in"} or \code{"both"}.
}
\examples{
\dontrun{
ex <- us_extract(ueis, years = 2008:2025, source = "archive")
ex$subawards <- us_fetch_subawards_in(ueis, 2008:2025)
}
}
