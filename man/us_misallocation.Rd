% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{us_misallocation}
\alias{us_misallocation}
\title{Misallocation share between an imputed and an actual annual series}
\usage{
us_misallocation(imputed, actual, normalize = TRUE)
}
\arguments{
\item{imputed, actual}{Numeric vectors over the same years.}

\item{normalize}{Rescale \code{imputed} to the actual total first.}
}
\value{
A scalar in \verb{[0, Inf)} (\code{NA} if \code{sum(actual) <= 0}); values at or
above 1 mean essentially none of the money was placed correctly.
}
\description{
\verb{0.5 * sum(|imputed - actual|) / sum(actual)}: the fraction of the
dollars placed in the wrong year. With \code{normalize = TRUE} the imputed
series is first rescaled to the actual total, isolating \emph{timing}; with
\code{FALSE} the method is also charged for missing the cash \emph{level}. See
\code{IMPUTATION.md} 3 for why this beats correlation for allocation tasks.
}
\examples{
us_misallocation(c(50, 50), c(10, 90))
}
