% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{us_schema}
\alias{us_schema}
\alias{us_empty}
\title{Canonical table schemas}
\usage{
us_schema(table = c("transactions", "subawards", "awards", "panel"))

us_empty(table = c("transactions", "subawards", "awards", "panel"))
}
\arguments{
\item{table}{Which schema to return.}
}
\value{
A \code{data.table} of \code{field} and \code{type} (\code{us_schema()}), or a zero-row
\code{data.table} with those columns (\code{us_empty()}).
}
\description{
\code{usaspend} normalizes every source into four tables. \code{us_schema()} returns the
column contract for one of them; \code{us_empty()} returns a correctly typed
zero-row table, which is what the not-yet-implemented normalization functions
will fill.
}
\details{
\describe{
\item{\code{transactions}}{One row per award action. The only grain that supports
an annual panel.}
\item{\code{subawards}}{One row per FSRS subaward report line.}
\item{\code{awards}}{One row per prime award -- the spine.}
\item{\code{panel}}{One row per organization x award x year -- the deliverable.}
}
}
\examples{
us_schema("panel")
str(us_empty("transactions"))
}
