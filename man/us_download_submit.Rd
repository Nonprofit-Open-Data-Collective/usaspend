% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-download.R
\name{us_download_submit}
\alias{us_download_submit}
\title{Submit a bulk transaction download job}
\usage{
us_download_submit(
  uei,
  award_types = us_award_type_codes("all"),
  start_date = "2007-10-01",
  end_date = Sys.Date()
)
}
\arguments{
\item{uei}{Character vector of UEIs (at most 20).}

\item{award_types}{Award type codes to include. Defaults to every type.}

\item{start_date, end_date}{Bounds on \code{action_date}. USAspending award search
is floored at 2007-10-01; earlier data needs the Custom Award Download or
the full database.}
}
\value{
A single string, the job file name, or \code{NA} if submission failed.
}
\description{
Submits one job to \verb{POST /api/v2/download/transactions/} and returns the
server-assigned file name, which is the job handle.
}
\details{
\code{recipient_search_text} is capped near 20 values by the API, and that cap is
undocumented. In practice large recipients time out server-side well below
the cap, so \code{\link[=us_download_run]{us_download_run()}} defaults to 5 UEIs per job and retries
failures one UEI at a time.
}
