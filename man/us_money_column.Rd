% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accounting.R
\name{us_money_column}
\alias{us_money_column}
\title{Which transaction amount counts as money}
\usage{
us_money_column(measure = c("obligation", "pragmatic", "outlay"))
}
\arguments{
\item{measure}{One of \code{"obligation"}, \code{"pragmatic"}, \code{"outlay"}.}
}
\value{
The canonical column name to sum.
}
\description{
USAspending carries several dollar columns and only some of them are revenue.
This returns the measure a given policy uses, so that the choice is made once
and visibly rather than being buried in an aggregation.
}
\details{
\describe{
\item{\code{"obligation"}}{\code{federal_action_obligation}. The signed amount the
government committed in this action. The default, and the only column
that supports an annual panel.}
\item{\code{"pragmatic"}}{\code{pragmatic_obligation} -- USAspending's own derived
measure, which substitutes the loan subsidy cost for loans and equals the
obligation otherwise. In the pilot the two are identical on every
non-loan row.}
\item{\code{"outlay"}}{Not available. \code{award_total_outlayed} exists but is
award-lifetime and mostly missing -- 91\% of pilot awards starting before
2017 report no outlay at all. There is no transaction-level or annual
outlay in the award data. Requesting this raises an error rather than
returning something misleading.}
}

The measures that must never be summed as revenue: \code{potential_value},
\code{base_and_all_options_value}, \code{current_total_value_of_award},
\code{total_funding_amount}. These are ceilings. The pilot holds $115bn of IDV
ceiling value against $0.75bn actually obligated on those vehicles --
a ceiling-based panel would overstate contract revenue by two orders of
magnitude.
}
\examples{
us_money_column("obligation")
}
